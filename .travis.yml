language: cpp
sudo: required
compiler:
- gcc
os:
- linux

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.9
    - gcc-4.9
    - cmake-3.4

script: cmake . && make && ./testci